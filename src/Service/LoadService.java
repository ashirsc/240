package Service;

import Request.LoadRequest;
import Result.LoadResult;

public class LoadService {

    /**
     *
     * Clears all data in the data base and then loads the posted user, person and event data
     * into the data base
     *
     * Objects contained in the request of person and event are added to the database.
     *
     * @param loadRequest
     * @return
     */
    LoadResult load(LoadRequest loadRequest) {

        return null;
    }
}
